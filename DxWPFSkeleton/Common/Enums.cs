namespace DXWPFSkeleton.Common
{
    public enum PLC_STATE
    {
        None = 0,
        Idle,
        Busy,
        Done,
        Error
    }

    public enum CaptureState
    {
        None = 0,
        Start,
        End,
    }
}
